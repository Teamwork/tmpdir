language: go
go:
  - 1.9.x
go_import_path: github.com/teamwork/tmpdir
notifications:
  email: false
cache:
  directories:
    - $HOME/gopath/pkg
install:
  - ./bin/setup-travis
script:
  - cd $HOME/gopath/src/github.com/teamwork/tmpdir
  - ./bin/test
  # No tests, so coverage is useless
  #- ./bin/coverage
  - ./bin/lint
